{
  "name": "ddns-docs",
  "version": "1.0.0",
  "description": "DDNS Documentation Website",
  "type": "module",
  "scripts": {
    "setup": "node doc/setup-docs.mjs",
    "docs:dev": "npm run setup && vitepress dev doc",
    "docs:build": "vitepress build doc",
    "docs:preview": "vitepress preview doc",
    "build": "npm run setup && npm run docs:build"
  },
  "devDependencies": {
    "vitepress": "^1.0.0"
  }
}
